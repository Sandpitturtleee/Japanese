from functions import radicals_main

if __name__ == "__main__":
    radicals_main()
    # # Find all writing components (often called "radicals") of the character
    # from jamdict import Jamdict
    #
    # jam = Jamdict()
    # kanji = '雲'
    # results = jam.krad[kanji]
    # print(results)
    #
    # meaning = jam.lookup('雲')
    # for c in meaning.chars:
    #     print(c, c.meanings())
    #
    # print()
    # for i in results:
    #     result = jam.lookup(i)
    #     for c in result.chars:
    #         print(c, c.meanings())
    #     print()